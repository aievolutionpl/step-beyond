# Step Beyond Benchmarks

This directory is reserved for executable benchmark harnesses. Until a runner is added, use `evals/README.md` for the manual A/B protocol and store results in `evals/results/`.
